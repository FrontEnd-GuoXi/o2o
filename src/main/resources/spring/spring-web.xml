<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd
    http://www.springframework.org/schema/mvc
    http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd">

    <mvc:annotation-driven />
    <!-- 关键：在子容器中也注册属性占位符解析器，以便解析 XML 中的 ${imgStore} -->
    <context:property-placeholder location="classpath:env/%env%/application.properties" ignore-unresolvable="true" />
    <mvc:resources mapping="/javaImages/**" location="${imgStore}" />
    <mvc:default-servlet-handler/>

    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/html/" />
        <property name="suffix" value=".html" />
    </bean>

    <!-- 文件上传解析器 -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="utf-8" />
        <!-- 1024 * 1024 * 20 = 20M -->
        <property name="maxUploadSize" value="20971520" />
        <property name="maxInMemorySize" value="20971520" />
    </bean>

    <context:component-scan base-package="com.o2o.web" />
    <!-- 让子容器扫描 com.o2o.config 以加载 WebMvcConfig -->
    <context:component-scan base-package="com.o2o.config" use-default-filters="false">
        <context:include-filter type="assignable" expression="com.o2o.config.WebMvcConfig"/>
    </context:component-scan>
</beans>